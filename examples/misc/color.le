use std/io;

fn main() {
    let color = 0xFFABE9FF;

    let r = (color >> 24) & 0xFF;
    let g = (color >> 16) & 0xFF;
    let b = (color >> 8) & 0xFF;
    let a = color & 0xFF;

    $dbg(r, g, b, a);
}
