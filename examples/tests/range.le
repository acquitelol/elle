use std/prelude;

fn main() {
    // Ranges create arrays for the time being
    $assert(5..10 == [5, 6, 7, 8, 9], nil);
    $assert(3..=8 == [3, 4, 5, 6, 7, 8], nil);
    $assert(0..0 == Array::new<i32>(), nil);

    io::println("All range tests have passed!".color("green").reset());
}
