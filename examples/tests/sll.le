// sll = singly linked list
use std/collections/sll;
use std/io;

fn main() {
    LinkedList<i32> list = LinkedList::new();
    defer list.free();

    list.add(10);
    list.add(23);
    list.add(2);
    list.add(42);
    list.add(39);

    io::println(list);
}
