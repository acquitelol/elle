use elle:io@{print};
use elle:int@{random};

const elle: String = "abc";

op main() {
    print(getStuff(5));
    ret a(b(c(d(e(1, 5))) + c(e())));
}

pub op getStuff(Int arg) -> Int {
    ret random(random(0, arg));
}