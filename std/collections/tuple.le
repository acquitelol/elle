global pub;

struct Tuple<T, U> {
    T x;
    U y;
};

fn Tuple::new<T, U>(T x, U y) {
    Tuple<T, U> *tuple = malloc(#size(Tuple<T, U>));
    *tuple = malloc(#size(Tuple<T, U>));

    tuple.x = x;
    tuple.y = y;

    return tuple;
}

struct Triple<T, U, V> {
    T x;
    U y;
    V z;
};
