use std/iter/core;
global pub;

fn Iterator::rev<T>(Iterator<T, DoubleEnded> self) -> Iterator<T, DoubleEnded> {
    return Iterator {
        env = self.env,
        next = self.next_back,
        next_back = self.next
    };
}
