// This test aims to ensure that passing lambdas which
// cannot have their inner type inferred, fails
fn foo<T>(fn(T) -> T cb) {
    return cb(39);
}

fn main() {
    foo(fn(x) x); // This should throw an error
}
