use std/io;

fn add(ElleMeta meta, ...args) {
    let res = 0;

    for let i = 0; i < meta.arity; i += 1 {
        res += args.yield(i32);
    }

    return res;
}

fn main() {
    let sum = add(1, 2, 3, 4, 5);
    $dbg(sum);
}
