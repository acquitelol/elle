use std/prelude;

fn add(ElleMeta meta, ...args) {
    res := 0;

    for i := 0; i < meta.arity; i += 1 {
        res += args.yield(i32);
    }

    return res;
}

fn main() {
    let sum = add(1, 2, 3, 4, 5);
    $dbg(sum);
}
