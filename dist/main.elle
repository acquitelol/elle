fn input(String message) {
    String out = "";
    puts(message);
    scanf!("%s", out);

    ret out;
}

pub fn main() {
    String test = input("input pls? :pleading:\n");
    printf!("\ndid you say %s? that's so true bestie\n", test);
}