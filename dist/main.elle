fn add(Int size, ...) {
    Int res = 0;
    Variadic args[size];
    Defer free(args);

    for _ = 0 to size - 1 {
        res += args yield Int;
    }

    return res;
}

pub fn main() {
    Int res = add.(1, 2, 3, 4);
    printf!("%d\n", res);
    return 0;
}
